```js Node.js
import Firecrawl from '@mendable/firecrawl-js';

const firecrawl = new Firecrawl({ apiKey: "fc-TU-CLAVE-DE-API" });

const active = await firecrawl.getActiveCrawls();
console.log(active);
```
