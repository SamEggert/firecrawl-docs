```js Node.js
const crawlResponse = await app.asyncCrawlUrl('https://firecrawl.dev', {
  limit: 100,
  scrapeOptions: {
    formats: ['markdown', 'html'],
  }
});

if (!crawlResponse.success) {
  throw new Error(`No se pudo rastrear: ${crawlResponse.error}`)
}

console.log(crawlResponse)
```
