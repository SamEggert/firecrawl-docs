```rust Rust
let map_result = app.map_url("https://firecrawl.dev", None).await;

match map_result {
    Ok(data) => println!("URLs identificadas: {:#?}", data),
    Err(e) => eprintln!("Error en el mapeo: {}", e),
}
```
