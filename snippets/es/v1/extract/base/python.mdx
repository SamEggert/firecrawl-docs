```python Python
from firecrawl import FirecrawlApp
from pydantic import BaseModel, Field

# Inicializa FirecrawlApp con tu clave de API
app = FirecrawlApp(api_key='tu_api_key')

class ExtractSchema(BaseModel):
    company_mission: str
    supports_sso: bool
    is_open_source: bool
    is_in_yc: bool

data = app.extract([
  'https://docs.firecrawl.dev/*', 
  'https://firecrawl.dev/', 
  'https://www.ycombinator.com/companies/'
], prompt='Extrae desde la página la misión de la empresa, si admite SSO, si es de código abierto y si está en Y Combinator.', schema=ExtractSchema.model_json_schema())
print(data)
```
