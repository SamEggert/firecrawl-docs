```python Python
from pydantic import BaseModel

class ExtractSchema(BaseModel):
    company_mission: str

# Define las URL de las que extraer datos
urls = [
    'https://firecrawl.dev/*'
]

# Define el prompt para la extracción
prompt = 'Extrae la misión de la empresa de la página.'

# Realiza la extracción
scrape_result = app.extract(urls, prompt=prompt, schema=ExtractSchema.model_json_schema())

# Verifica si la extracción fue correcta
if not scrape_result.success:
    raise Exception(f"Error al raspar: {scrape_result.error}")

# Imprime los datos extraídos
print(scrape_result.data)
```
