---
title: 'Rust'
description: 'El SDK de Firecrawl para Rust es una biblioteca que te ayuda a extraer y rastrear sitios web fácilmente, y a generar los datos en un formato listo para usar con modelos de lenguaje (LLM).'
icon: 'rust'
og:title: "SDK de Rust | Firecrawl"
og:description: "El SDK de Firecrawl para Rust es una biblioteca que te ayuda a extraer y rastrear sitios web fácilmente, y a generar los datos en un formato listo para usar con modelos de lenguaje (LLM)."
---

import InstallationRust from '/snippets/es/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/es/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/es/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/es/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/es/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/es/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/es/v1/llm-extract/base/rust.mdx'

<Warning>
  Este SDK utiliza actualmente la versión **v1** de la API de Firecrawl, que no es la más reciente (v2 está disponible). Algunas funciones y mejoras pueden estar disponibles únicamente en v2.
</Warning>


<div id="installation">
  ## Instalación
</div>

Para instalar el SDK de Rust de Firecrawl, añade lo siguiente a tu `Cargo.toml`:

<InstallationRust />

<div id="usage">
  ## Uso
</div>

Primero, obtén una clave de API en [firecrawl.dev](https://firecrawl.dev). Luego, inicializa `FirecrawlApp`. A partir de ahí, podrás acceder a funciones como `FirecrawlApp::scrape_url`, que te permiten usar nuestra API.

Aquí tienes un ejemplo de cómo usar el SDK en Rust:

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### Extracción de una URL
</div>

Para extraer una sola URL, usa el método `scrape_url`. Recibe la URL como parámetro y devuelve los datos extraídos como un `Document`.

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### Raspado con Extract
</div>

Con Extract, puedes extraer fácilmente datos estructurados de cualquier URL. Debes especificar el esquema en formato JSON Schema, usando la macro `serde_json::json!`.

<LLMExtractRust />

<div id="crawling-a-website">
  ### Rastreo de un sitio web
</div>

Para rastrear un sitio web, utiliza el método `crawl_url`. Este método espera a que el rastreo termine, lo cual puede llevar bastante tiempo según la URL de inicio y las opciones configuradas.

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### Rastreo asíncrono
</div>

Para rastrear sin esperar el resultado, usa el método `crawl_url_async`. Acepta los mismos parámetros, pero devuelve una estructura `CrawlAsyncRespone` que contiene el ID del rastreo. Puedes usar ese ID con el método `check_crawl_status` para consultar el estado en cualquier momento. Ten en cuenta que los rastreos completados se eliminan después de 24 horas.

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### Mapear una URL
</div>

Identifica todos los enlaces asociados a partir de una URL de inicio.

<MapRustShort />

<div id="error-handling">
  ## Manejo de errores
</div>

El SDK gestiona los errores devueltos por la API de Firecrawl y por nuestras dependencias, y los unifica en el enum `FirecrawlError`, que implementa `Error`, `Debug` y `Display`. Todos nuestros métodos devuelven un `Result<T, FirecrawlError>`.