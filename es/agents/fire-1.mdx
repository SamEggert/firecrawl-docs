---
title: "FIRE-1 (Agente) (Beta)"
sidebarTitle: "FIRE-1 (Beta)"
description: "Agente de IA que habilita la navegación e interacción inteligentes con páginas web"
og:title: "FIRE-1 Agente de IA (Beta) | Firecrawl"
og:description: "FIRE-1 es un agente de IA que habilita la navegación e interacción inteligentes con páginas web"
icon: "robot"
---

import ScrapeFire1Python from "/snippets/es/v1/scrape/agent-f1/python.mdx";
import ScrapeFire1Node from "/snippets/es/v1/scrape/agent-f1/js.mdx";
import ScrapeFire1CURL from "/snippets/es/v1/scrape/agent-f1/curl.mdx";
import ExtractFire1Python from "/snippets/es/v1/extract/agent-f1/python.mdx";
import ExtractFire1Node from "/snippets/es/v1/extract/agent-f1/js.mdx";
import ExtractFire1CURL from "/snippets/es/v1/extract/agent-f1/curl.mdx";

FIRE-1 es un agente de IA que amplía las capacidades de raspado de Firecrawl. Puede controlar acciones del navegador y navegar por estructuras web complejas para habilitar una extracción de datos más completa que la que permiten los métodos de raspado tradicionales.


<div id="what-fire-1-can-do">
  ### Qué puede hacer FIRE-1:
</div>

- Planificar y ejecutar acciones para descubrir datos
- Interactuar con botones, enlaces, campos de entrada y elementos dinámicos.
- Obtener varias páginas de datos que requieren paginación, múltiples pasos, etc.

<div id="how-to-enable-fire-1">
  ## Cómo habilitar FIRE-1
</div>

Activar FIRE-1 es sencillo. Simplemente incluye un objeto `agent` en tu solicitud a la API de scraping:

```json
"agent": {
  "model": "FIRE-1",
  "prompt": "Aquí van tus instrucciones detalladas de navegación."
}
```

*Nota:* El campo `prompt` es obligatorio para las solicitudes de raspado, ya que indica a FIRE-1 exactamente cómo interactuar con la página web. Para `/extract`, usará el prompt proporcionado en el parámetro `prompt` del cuerpo de la solicitud, por lo que puedes omitir el campo `agent.prompt` anterior.


<div id="example-usage-with-scrape-endpoint">
  ## Ejemplo de uso con el endpoint de Scrape
</div>

Aquí tienes un ejemplo rápido que usa FIRE-1 con el endpoint de scrape para obtener las empresas en el espacio de Consumer de Y Combinator:

<CodeGroup>

<ScrapeFire1Python />
<ScrapeFire1Node />
<ScrapeFire1CURL />

</CodeGroup>

En este escenario, FIRE-1 hace clic de forma inteligente en el botón W22, en el botón Consumer y extrae los datos de las empresas.

<div id="using-fire-1-with-the-extract-endpoint">
  ## Uso de FIRE-1 con el endpoint Extract
</div>

De manera similar, puedes usar el agente FIRE-1 con el endpoint `/v1/extract` para tareas de extracción complejas que requieren navegar por varias páginas o interactuar con elementos.

**Ejemplo:**

<CodeGroup>

<ExtractFire1Python />
<ExtractFire1Node />
<ExtractFire1CURL />

</CodeGroup>

> FIRE-1 ya está disponible y en fase de vista previa.

<div id="billing">
  ## Facturación
</div>

| Endpoint   | Costo base               | Costo del agente (versión preliminar) | Notas                                      |
|------------|--------------------------|----------------------------------------|--------------------------------------------|
| `/scrape`  | 150 créditos por página  | 0–900 créditos de agente por página    | Varía según la complejidad de la tarea.    |
| `/extract` | Consulta la calculadora de tokens | ~8x extracción sin agente        | Usa precios basados en tokens.             |

- **¿Por qué FIRE-1 es más caro?**  
  FIRE-1 utiliza automatización avanzada del navegador y planificación con IA para interactuar con páginas web complejas, lo que requiere más recursos de cómputo que una extracción estándar.

> **Nota:** FIRE-1 se encuentra actualmente en versión preliminar. Los precios y los límites pueden cambiar. Para ver los detalles más recientes sobre los costos de `/extract`, consulta nuestra [calculadora de tokens](https://www.firecrawl.dev/pricing?extract-pricing=true#token-calculator).

<div id="rate-limits">
  ## Límites de tasa
</div>

* `/scrape`: 10 solicitudes por minuto
* `/extract`: 10 solicitudes por minuto